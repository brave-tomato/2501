'use client';
import React, { FC } from 'react';

import { ICustomComponentProps } from '@/types';

import { Flex } from 'antd';

/**
 * media 我是媒体
 */
const Media: FC<ICustomComponentProps> = () => {
    return <Flex>Media</Flex>;
};

export default Media;
